<UserControl x:Class="SGRC.App.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">
    <Grid Background="#2D2D2D">
        <materialDesign:Card Width="350" Padding="30" VerticalAlignment="Center">
            <StackPanel>
                <TextBlock Text="SGRC" FontSize="32" FontWeight="Bold" HorizontalAlignment="Center" Foreground="White" Margin="0,0,0,20"/>
                <TextBlock Text="Ingrese sus credenciales" HorizontalAlignment="Center" Margin="0,0,0,20"/>
                
                <TextBox x:Name="txtUsuario" materialDesign:HintAssist.Hint="Usuario" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Margin="0,10"/>
                <PasswordBox x:Name="txtPassword" materialDesign:HintAssist.Hint="Contraseña" materialDesign:HintAssist.IsFloating="True" Margin="0,10,0,30"/>                <Button x:Name="btnLogin" Content="ENTRAR AL SISTEMA" Click="BtnLogin_Click"/>
            </StackPanel>
        </materialDesign:Card>
    </Grid>
</UserControl>